# next-auth@4 declares an optional peer of nodemailer@^7, but the app uses
# nodemailer@9 directly (src/lib/email.ts), not next-auth's email provider.
# Allow the newer version without blocking installs.
legacy-peer-deps=true
